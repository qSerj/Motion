# Дорожная карта (живой документ)

## Сейчас (стабилизация ядра и форматов)
- Зафиксировать и покрыть тестами IPC‑протокол (docs/ipc.md + tests).
- Стабилизировать чтение/запись `.mtp v2` (round‑trip тесты + фикстуры).
- Выделить чистую логику из realtime‑циклов для детерминированных тестов:
  - расписание оверлеев;
  - функции скоринга;
  - парсинг/валидация таймлайна.
- Улучшить путь «редактор → сохранение → повторное открытие» для таймлайна.

## Далее (контент и редактор)
- Полный цикл «Digitizer → .mtp»: оцифровка видеофайла в таймлайн/паттерны.
- Расширение типов оверлеев (картинки, трансформации, позиционирование).
- Более понятные ошибки для некорректных `.mtp` файлов.

## Потом (режимы и масштабирование)
- Party Mode: мульти‑персонный режим и UI для нескольких игроков.
- Метроном, управление скоростью, сценарии терапевтических упражнений.

## Дальше (API и расширения)
- Выделение ядра в API‑слой при необходимости (HTTP/WebSocket — только если ZMQ станет узким местом).
- VR/3D‑расширения и адаптеры под новые источники данных.
